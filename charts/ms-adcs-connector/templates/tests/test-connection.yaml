apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-adcs-connector.fullname" . }}-test-connection"
  labels:
    {{- include "ms-adcs-connector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ms-adcs-connector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
