apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fe-administrator.fullname" . }}-test-connection"
  labels:
    {{- include "fe-administrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fe-administrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
