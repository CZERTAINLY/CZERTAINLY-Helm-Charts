apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "common-credential-provider.fullname" . }}-test-connection"
  labels:
    {{- include "common-credential-provider.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "common-credential-provider.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
