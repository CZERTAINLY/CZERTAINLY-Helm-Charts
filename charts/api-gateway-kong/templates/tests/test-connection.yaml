apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-gateway-kong.fullname" . }}-test-connection"
  labels:
    {{- include "api-gateway-kong.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-gateway-kong.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
