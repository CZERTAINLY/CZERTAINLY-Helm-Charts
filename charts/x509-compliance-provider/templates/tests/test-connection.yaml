apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "x509-compliance-provider.fullname" . }}-test-connection"
  labels:
    {{- include "x509-compliance-provider.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "x509-compliance-provider.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
