apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keystore-entity-provider.fullname" . }}-test-connection"
  labels:
    {{- include "keystore-entity-provider.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keystore-entity-provider.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
