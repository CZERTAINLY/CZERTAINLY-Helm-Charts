apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auth-opa-policies.fullname" . }}-test-connection"
  labels:
    {{- include "auth-opa-policies.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auth-opa-policies.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
