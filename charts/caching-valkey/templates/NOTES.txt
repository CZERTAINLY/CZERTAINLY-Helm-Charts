CHART NAME: {{ .Chart.Name }}
CHART VERSION: {{ .Chart.Version }}
APP VERSION: {{ .Chart.AppVersion }}

** Please be patient while the chart is being deployed **

CZERTAINLY Valkey Caching Service has been installed.

Valkey is running at: {{ include "caching-valkey.fullname" . }}:{{ .Values.service.port }}

To verify the installation:
  kubectl exec -it deployment/{{ include "caching-valkey.fullname" . }} -- valkey-cli ping

{{- if pluck "password" .Values.global.valkey .Values.valkey | compact | first }}

Note: Valkey is password-protected. Use -a flag with valkey-cli:
  kubectl exec -it deployment/{{ include "caching-valkey.fullname" . }} -- valkey-cli -a "$VALKEY_PASSWORD" ping
{{- end }}

For more information, visit: https://docs.czertainly.com
