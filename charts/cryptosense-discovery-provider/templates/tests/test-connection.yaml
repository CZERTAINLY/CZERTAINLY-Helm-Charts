apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cryptosense-discovery-provider.fullname" . }}-test-connection"
  labels:
    {{- include "cryptosense-discovery-provider.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cryptosense-discovery-provider.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
